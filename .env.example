# Copy to .env for local overrides (optional). .env is gitignored.
# CI: set BASE_URL as a GitHub repository variable (Settings → Secrets and variables → Actions → Variables).

BASE_URL=https://the-internet.herokuapp.com
JS_PROMPT_NAME=Anuradha Rathnayake
DROPDOWN_OPTION=Option 2
